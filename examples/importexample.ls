from "sampleimport" import add as addStrs;

func add(int i, int n) : int {
    return (i + n);
}

func openFile (str name) : void {
    return;
}
print(openFile(""));
print(addStrs(2, 3));
print(add(2, 3));